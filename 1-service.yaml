# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: blog-app
#   namespace: default
#   labels:
#     app: blog-app
# spec:
#   ports:
#     - port: 8080
#   selector:
#     app: blog-app
---
apiVersion: v1
kind: Service
metadata:
  name: lb-service
  labels:
    app: lb-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: blog-app